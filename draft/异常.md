# Java 异常学习路线

## 1. 异常是什么
- 为什么程序会出现异常
- 异常和普通错误的区别
- 异常发生后程序会怎样

## 2. Java 异常体系
- `Throwable`
- `Error`
- `Exception`
- `RuntimeException`
- 它们之间的继承关系

## 3. Error 和 Exception 的区别
- `OutOfMemoryError`
- `StackOverflowError`
- 为什么一般不处理 `Error`

## 4. 受检异常 Checked Exception
- 什么是编译期异常
- 为什么必须处理
- 常见例子：`IOException`

## 5. 非受检异常 Unchecked Exception
- 什么是运行时异常
- `RuntimeException`
- 常见例子：
  - `NullPointerException`
  - `ArithmeticException`
  - `IndexOutOfBoundsException`
  - `ClassCastException`

## 6. try-catch 基础
- `try` 做什么
- `catch` 做什么
- 异常发生后的执行顺序

## 7. 多个 catch
- 多种异常分别处理
- 父类异常和子类异常顺序
- 为什么子类异常要写在前面

## 8. finally
- `finally` 的作用
- 什么情况下会执行
- `return` 和 `finally`
- `System.exit()`

## 9. throw
- 主动抛出异常
- `throw new RuntimeException(...)`

## 10. throws
- 把异常交给调用者
- `throws IOException`
- `throw` 和 `throws` 的区别

## 11. 异常传播
- A 调用 B，B 调用 C
- C 抛异常后如何逐层向上传播
- 谁负责处理异常

## 12. 异常栈 Stack Trace
- 怎么看报错日志
- `at xxx.java:25`
- 如何定位真正出错的位置
- `Caused by`

## 13. 自定义异常
- 为什么需要自定义异常
- 继承 `RuntimeException`
- 业务异常如何设计

## 14. 异常和返回值的区别
- `return false`
- `return null`
- 抛异常
- 各自在什么场景使用

## 15. 异常是否会导致程序崩溃
- 当前方法是否结束
- 当前线程是否结束
- JVM 是否结束
- Spring Boot 服务是否会挂

## 16. 异常和线程
- 子线程异常
- 主线程异常
- 一个线程异常是否影响其他线程

## 17. 异常和事务
- `@Transactional`
- 什么异常会回滚
- Checked Exception 为什么默认不回滚
- `rollbackFor`

## 18. 异常和 Spring MVC
- Controller 抛异常后会怎样
- HTTP 500 怎么来的
- `@ExceptionHandler`
- `@RestControllerAdvice`

## 19. 异常和资源释放
- `finally` 关闭资源
- `try-with-resources`
- 为什么更推荐 `try-with-resources`

## 20. 企业开发中的异常规范
- DAO 层异常
- Service 层异常
- Controller 层异常
- 统一异常处理
- 日志应该在哪里打印
- 避免重复打印异常

## 21. 异常设计中的常见坑
- 到处写 `catch (Exception e)`
- 吞异常
- `finally` 中写 `return`
- 只打印 `e.getMessage()`
- 用异常控制正常业务流程

## 22. 异常底层原理
- JVM 如何寻找 `catch`
- 方法调用栈
- 栈帧展开
- 为什么抛异常的成本通常比普通 `return` 更高

# 推荐学习顺序

异常体系  
→ `try-catch`  
→ `finally`  
→ `throw / throws`  
→ 异常传播  
→ Stack Trace  
→ 自定义异常  
→ Spring 企业开发中的异常处理